export const metadata = {
  title: {
    absolute: "تکمیل اطلاعات کاربری",
  },
  description: "",
};
function Layout({ children }) {
  return (
    <div className="flex items-center">
      <div className="w-full bg-secondary-0">{children}</div>
    </div>
  );
}

export default Layout;
